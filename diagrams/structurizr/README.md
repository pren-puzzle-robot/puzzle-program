# Structurizr

`workspace.dsl` is the source of truth for the Puzzle Program C4 model.

It contains:

- system context view
- container view
- runtime dynamic view
- production deployment view

Generated Structurizr state is ignored by `.gitignore`.
